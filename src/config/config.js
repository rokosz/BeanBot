const moment = require('moment');

const config = {
  "token": "you thought.",
  "googleAPI": "TBD on use",
}

exports.db = 'yeet';
exports.prefixArray = ['b.', 'b!', '-b'];
exports.supportArray = ['userid array here'];
exports.timestamp = `${moment().format("YYYY-MM-DD HH:mm:ss")}`;
exports.adminRoles = ['', ''];
